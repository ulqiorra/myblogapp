@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, IList<MyBlog.Core.Objects.Tag> tags)
{
  foreach (var tag in tags)
  {
    <div class="tags-div">@MyBlog.ActionLinkExtensions.TagLink(htmlHelper, tag)</div>     
  }
}